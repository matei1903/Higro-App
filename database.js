/*const XLSX = require("xlsx");

const workbook = XLSX.readFile("higrodb.xlsx");
const workshett = workbook.Sheets("Sheet1");

const denumire = XLSX.utils.sheet_to_json(worksheet);
console.log(denumire);
*/